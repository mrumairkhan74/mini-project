<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Update Post</title>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <div class="flex justify-end items-end">
        <a
          href="/logout"
          class="text-sm bg-red-500 text-center px-3 py-2 rounded"
          >Logout</a
        >
      </div>
      <h5 class="mb-2 text-3xl px-10">Update Post</h5>
      <form
        class="flex flex-col justify-start items-start px-10"
        action="/update/<%=post._id%>"
        method="post"
      >
        <textarea
          name="content"
          class="resize-none rounded bg-zinc-800 px-5 py-3 mb-2 w-full h-40"
        ><%= post.content %></textarea>
        <input
          type="submit"
          class="bg-blue-500 px-3 py-1 rounded text-sm"
          value="Update Post"
        />
      </form>
    </div>
  </body>
</html>
